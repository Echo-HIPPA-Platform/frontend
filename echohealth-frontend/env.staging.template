# Staging Environment Template
# Copy this file to .env and update the values

# Replace with your actual Paystack PUBLIC key (starts with pk_live_ or pk_test_)
# Get this from your Paystack Dashboard -> Settings -> API Keys & Webhooks
NEXT_PUBLIC_PAYSTACK_PUBLIC_KEY=your_paystack_public_key_here

# Staging backend URL (clean, no ports)
NEXT_PUBLIC_API_BASE_URL=http://staging.echopsychology.com

# Environment identifier
NEXT_PUBLIC_ENVIRONMENT=staging

# IMPORTANT: Never put secret keys (sk_) in environment variables that start with NEXT_PUBLIC_
# Secret keys should only be used on the server-side for payment verification
